@extends('layouts.app')

@section('content')
    <main class="mx-auto max-w-5xl px-5 py-12"><a href="{{ route('home') }}" class="text-sm font-semibold text-emerald-700">← الرئيسية</a><h1 class="mt-5 text-3xl font-bold">نتائج البحث</h1><form action="{{ route('search') }}" class="mt-6 flex rounded-xl border border-slate-300 bg-white p-1"><input name="q" value="{{ $query }}" class="min-w-0 flex-1 border-0 px-4 focus:ring-0" placeholder="ابحث في المحتوى"><button class="rounded-lg bg-emerald-700 px-5 py-2 text-white">بحث</button></form><p class="mt-5 text-sm text-slate-500">{{ $query ? "نتائج البحث عن: {$query}" : 'أدخل عبارة للبحث.' }}</p><div class="mt-6 grid gap-4 sm:grid-cols-2">@forelse($items as $item)<article class="rounded-xl border border-slate-200 bg-white p-5"><span class="text-xs text-emerald-700">{{ $item->type }}</span><h2 class="mt-2 text-lg font-bold">{{ $item->title }}</h2><p class="mt-2 text-sm leading-6 text-slate-600">{{ $item->excerpt }}</p></article>@empty<p class="rounded-xl bg-slate-100 p-6 text-center text-slate-500">لا توجد نتائج مطابقة.</p>@endforelse</div><div class="mt-7">{{ $items->links() }}</div></main>
@endsection
